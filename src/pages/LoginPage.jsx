import LoginForm from "../components/LoginForm";
import Logo from "../components/Logo";

const LoginPage = () => {
  return (
    <div>
      <Logo/>
      <LoginForm />
    </div>
  );
};

export default LoginPage;
